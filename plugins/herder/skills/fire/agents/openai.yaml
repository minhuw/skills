interface:
  display_name: "Herder Fire"
  short_description: "Execute bounded plans natively or through Orca"
  default_prompt: "Use $herder:fire to execute, resume, inspect, or safely clean a validated semantically bounded Herder plan graph through the deterministic local Run Manager and its native worker transport."
